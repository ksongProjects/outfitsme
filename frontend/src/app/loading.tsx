import AppLoadingScreen from "@/components/app/AppLoadingScreen";

export default function Loading() {
  return (
    <AppLoadingScreen
      title="Opening your style workspace"
      subtitle="Loading the next screen and restoring your polished outfit workflow."
    />
  );
}
