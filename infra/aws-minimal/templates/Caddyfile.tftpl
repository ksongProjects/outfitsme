%{ if enable_tls ~}
%{ if tls_email != "" ~}
{
  email ${tls_email}
}
%{ endif ~}

${domain_name} {
%{ else ~}
{
  auto_https off
}

:80 {
%{ endif ~}
  encode gzip
  reverse_proxy /api/* backend:5000
  reverse_proxy frontend:3000
}
